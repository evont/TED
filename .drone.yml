---
kind: pipeline
type: docker
name: default

workspace:
  base: /src
  path: /egg

steps:
- name: egg-build
  image: node
  commands:
    - pwd
    - npm i --production --registry=https://registry.npm.taobao.org
    - ls

trigger:
  branch:
  - master
  event:
  - push